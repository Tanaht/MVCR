<!DOCTYPE html>
<html>
<head>
	<title>{{title}}</title>
</head>
<body>
	<div class="yu-header">
		{{ header }}
	</div>
	{{ menu }}
	{{ arianne }}
	{{ content }}
	<div class="yu-footer">
		{{ footer }}
	</div>
</body>
</html>